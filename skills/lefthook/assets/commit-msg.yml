prepare-commit-msg:
  commands:
    commitzen:
      interactive: true
      run: pnpm exec cz --hook
      env:
        LEFTHOOK: "0"

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
